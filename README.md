# Simple Sinatra/CoffeeScript Application

Write Ruby in `app.rb`.

Write CoffeeScript in `app.js.coffee`.

Edit HTML in `app.html`.

Run with `shotgun app.rb`.

Visit `http://localhost:9393` to see your application.
